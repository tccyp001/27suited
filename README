1: start backend, following command:
`~/27suited/node-backend$ node server.js`
OR run and debug in vscode! (recommanded).

2: start frontend: 
npm start run 


Build:
change the baseUrl first. common.tsx
~/27suited/frontend$ npm run build

don't forget to add missing files


Server:
in backend folder
sudo forever list
sudo forever stop 0
sudo forever start server.js